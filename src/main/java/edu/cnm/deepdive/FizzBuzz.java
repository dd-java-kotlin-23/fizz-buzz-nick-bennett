package edu.cnm.deepdive;

/**
 * Implements the convert method for constructing the FizzBuzz representation of a numeric value.
 * <p>Rules of FizzBuzz:</p>
 * <ul>
 *   <li>An integer which is not divisible by either 3 or 5 is converted to the string representation of that number.</li>
 *   <li>An integer which is divisible by 3, but not by 5, is represented as "Fizz".</li>
 *   <li>An integer which is divisible by 5, but not by 3, is represented as "Buzz".</li>
 *   <li>An integer which is divisible by both 3 and 5 is represented as "FizzBuzz".</li>
 * </ul>
 */
public class FizzBuzz {

  /**
   * Converts an int to its equivalent FizzBuzz string representation.
   *
   * @param value Input to be converted.
   * @return FizzBuzz representatio of input.
   */
  public String convert(int value) {
    throw new UnsupportedOperationException("Not yet implemented.");
  }

}
